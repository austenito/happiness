.container
  .row
    .col-sm-12
      = form_for :survey do |f|
        = f.submit "Create survey", class: 'btn btn-primary'

  .row
    .col-sm-12
      .happiness-per-day-chart(ng-controller='dayChartsController')
      .happiness-per-place-chart(ng-controller='placeChartsController')

